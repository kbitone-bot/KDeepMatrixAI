# KDeepMatrixAI × Synthetic Data Quality Platform
## 국방 AI/빅데이터 사업 대응 컨설팅 보고서

> 작성일: 2026-04-24  
> 대상: ICTWAY 기/현 수행 사업 기반 실증 시범 서비스 제안  
> 핵심 논의: "군의 실질적인 데이터 수급 및 합성 데이터를 기반으로 실증 시범 서비스를 구축할 수 있는지"

---

## 1. 현재 보유 역량 분석

### 1.1 자산 ①: 합성데이터 생성·검증 플랫폼
**Repo**: `kbitone-bot/synthetic-data-quality-platform`

| 역량 | 상세 | 사업 대응도 |
|---|---|---|
| **합성데이터 생성** | statistical / perturbation / model_based(SDV GaussianCopula) 3가지 모드 | ⭐⭐⭐⭐☆ |
| **품질 검증** | 6차원 평가(분포·상관·프라이버시·제약·결측·ML Utility) | ⭐⭐⭐⭐⭐ |
| **ML 적합성 판정** | TSTR(Train Synthetic, Test Real) 프레임워크 | ⭐⭐⭐⭐⭐ |
| **자동 판정** | PASS/REVIEW/FAIL 3단계 + 치명적 위반 자동 감지 | ⭐⭐⭐⭐☆ |
| **UI/UX** | Streamlit 5탭 + 한국어 네이티브 + Plotly 시각화 | ⭐⭐⭐⭐⭐ |
| **CLI 자동화** | `run_pipeline.py` 배치 실행 | ⭐⭐⭐⭐☆ |
| **리포팅** | JSON(기계판독) + HTML(인터랙티브 대시보드) | ⭐⭐⭐⭐☆ |

**강점**: "생성"이 아닌 **"검증·품질보증(QA)"** 중심의 차별화  
**한계**: 생성된 합성데이터를 **실제 분석모델/AI 모델의 입력으로 연결하는 파이프라인**은 미구현

### 1.2 자산 ②: 빅데이터 분석·가시화 통합 플랫폼 (KDeepMatrixAI)
**Repo**: `kbitone-bot/KDeepMatrixAI`

| 역량 | 상세 | 사업 대응도 |
|---|---|---|
| **모델 자동 인식** | `af_ba_req_*` 패턴 스캔, 메타데이터 수집 | ⭐⭐⭐⭐☆ |
| **RAM 분석 (001)** | MTBF/MTTR/고장률/수리율/가용도 + lifelines 분포적합 | ⭐⭐⭐⭐⭐ |
| **수명예측 (002)** | scipy/Fitter 분포적합 + B10/B50 수명 + CDF/PDF 시각화 | ⭐⭐⭐⭐⭐ |
| **시험소예측 (004)** | GradientBoostingRegressor Serving + PI + 역추정 | ⭐⭐⭐⭐⭐ |
| **유사부품추천 (005)** | KMeans+TF-IDF + 코사인 유사도 Top-N 검색 | ⭐⭐⭐⭐⭐ |
| **IMQC인원수급 (007)** | 등급현황+계획수립 병합, 현재/필요 인원 비교 | ⭐⭐⭐⭐⭐ |
| **시각화** | Plotly 인터랙티브 차트(5개 모델 전체 지원) | ⭐⭐⭐⭐⭐ |
| **API 서버** | FastAPI 기반 REST API (`/api/v1/analyze/*`) | ⭐⭐⭐⭐⭐ |
| **웹 UI** | Streamlit 기반 모델 선택·조건 입력·결과 표시 | ⭐⭐⭐⭐⭐ |
| **결과 관리** | `outputs/{analysis_id}/` CSV + HTML 보고서 | ⭐⭐⭐⭐☆ |

**강점**: 기존 분석모델을 **수정하지 않고** 신규 코드만으로 웹 통합 실행  
**한계**: **실시간 모델 Serving**이 아닌 Ad-hoc 배치 분석 구조

---

## 2. 사업 요구사항 대비 갭(Gap) 분석

사업측에서 문의한 5가지 핵심 질의에 대한 현재 상태:

### 질의 1: "5대 AI 모델을 국방 도메인 특화로 학습된 모델(Binary 파일)로 보유하고 있는지"

| 모델 | 현재 상태 | Binary 보유 | 도메인 특화 학습 | 갭 |
|---|---|---|---|---|
| af_ba_req_001 (RAM) | ✅ 완전 구현 | ❌ 없음 (코드 기반 분석) | ❌ 통계 분포적합만 수행 | 중간 |
| af_ba_req_002 (수명예측) | ✅ 완전 구현 | ❌ 없음 (코드 기반 분석) | ❌ scipy/Fitter 분포적합 | 중간 |
| af_ba_req_004 (시뮬레이션) | ✅ 완전 구현 | ✅ `.joblib` 보유 (8시험소) | ✅ GradientBoosting 회귀 | 낮음 |
| af_ba_req_005 (유사부품) | ✅ 완전 구현 | ✅ `.joblib` 보유 (KMeans+TF-IDF) | ✅ 비지도 학습 클러스터링 | 낮음 |
| af_ba_req_007 (IMQC) | ✅ 완전 구현 | ❌ 없음 (코드 기반 분석) | ❌ 집계/산출 로직 | 중간 |

**현재 상태: 2개 모델은 Binary 보유, 3개 모델은 코드 기반 분석**

| 모델 | Binary 타입 | 경로 |
|---|---|---|
| af_ba_req_004 | `scaler.joblib`, `model.joblib`, `calibration.joblib` | `outputs/models_004/시험소008/` 등 |
| af_ba_req_005 | `cluster_model_kmeans.joblib` | `outputs/models_005/` |

> 📌 **필요 조치**: 001/002/007을 **머신러닝 모델로 재구현**하여 `.joblib` Binary를 생성하면 5개 모델 전체가 AI 모델 Binary를 보유하게 됨.

---

### 질의 2: "국방지능형플랫폼 Serving 영역에 배포하여 Inference까지 수행 가능한지"

| 요구 항목 | 현재 상태 | 필요 기술/플랫폼 |
|---|---|---|
| 모델 패키징 (Binary) | ✅ 2개 모델 (.joblib) | pickle, joblib, ONNX |
| 모델 레지스트리 | ❌ 없음 | MLflow Model Registry, DVC |
| REST API Serving | ✅ FastAPI + 직접 Serving | BentoML, Triton, TorchServe |
| 컨테이너화 | ❌ 없음 | Docker, Kubernetes |
| A/B 테스트 | ❌ 없음 | Istio, FlagSmith |
| 모니터링 | ❌ 없음 | Prometheus, Grafana |
| 버전 관리 | ❌ 없음 | MLflow, DVC |

**현재 KDeepMatrixAI의 FastAPI는 "분석 실행 API"이지 "모델 Inference API"가 아님**

> 📌 **필요 조치**: 
> 1. 학습된 모델 Binary를 산출
> 2. BentoML 또는 순수 FastAPI로 **모델 Serving 서비스**를 별도 구축
> 3. Docker 이미지화 + Kubernetes Manifest 작성
> 4. 국방지능형플랫폼의 표준 API 스펙(요청/응답 포맷)을 준수하도록 어댑터 개발

---

### 질의 3: "시뮬레이션 데이터 보유 여부"

| 모델 | 시뮬레이션 데이터 존재 여부 | 상태 |
|---|---|---|
| af_ba_req_001 | ✅ `가용도분석자료.xlsb` (327,699행) | 실데이터 |
| af_ba_req_002 | ✅ `정밀측정폐품현황.xlsb` (31,206행) | 실데이터 |
| af_ba_req_004 | ✅ `inferenceSet.csv` + `valueRatio.csv` (8K샘플/시험소) | **합성 데이터로 학습 완료** |
| af_ba_req_005 | ✅ `정밀측정품목현황.xlsb` (9.8MB) | 실데이터 |
| af_ba_req_007 | ✅ `21-25년 계획수립현황.xlsx` (465,372행) | 실데이터 |

**af_ba_req_004는 합성 데이터(8K샘플/시험소)로 GBR 학습 및 Serving 완료**

> ✅ **완료된 작업**: 
> 1. `scripts/train_004.py`로 시험소별 8,000개 합성 샘플 생성
> 2. GradientBoostingRegressor 학습 → `scaler.joblib`, `model.joblib`, `calibration.joblib` 산출
> 3. `backend/services/sim_service.py`로 실시간 Inference API 구현
> 4. Streamlit UI에서 입력 → 예측 → PI(예측구간) 표시

---

### 질의 4: "실 구축 후 해당 데이터를 이용한 검증 시나리오 사례와 시연"

현재 보유한 **검증** 역량:
- ✅ 합성데이터 품질 검증 (6차원, TSTR)
- ✅ RAM 분석 결과 시각화 검증
- ✅ **합성데이터 → AI 모델 학습 → 실데이터 Inference** 파이프라인 구현 (004)
- ⚠️ **시뮬레이션 데이터 vs 실데이터 vs 합성데이터** 3자 비교 시나리오 — Streamlit UI 통합 필요

> 📌 **필요 조치**: 
> 1. "실데이터 → 합성데이터 생성 → 동일 모델로 각각 학습 → 동일 테스트셋으로 성능 비교" 파이프라인 구축
> 2. af_ba_req_004를 시연용 핵심 모델로 선정하여 **실데이터 예측 vs 시뮬레이션 예측 vs 합성데이터 기반 예측**을 한 화면에서 비교하는 데모 개발

---

## 3. 두 플랫폼 연계 아키텍처 제안

### 3.1 통합 플랫폼 개념도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        국방지능형플랫폼 (DAPA AI Platform)                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Model Serving Zone                               │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │   │
│  │  │ RAM Model  │  │ 수명예측   │  │ 시뮬레이션 │  │ 유사부품   │  │   │
│  │  │ (ONNX)     │  │ (joblib)   │  │ (XGBoost)  │  │ (TF-IDF)   │  │   │
│  │  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  │   │
│  │        │               │               │               │         │   │
│  │        └───────────────┴───────────────┴───────────────┘         │   │
│  │                        │                                          │   │
│  │              ┌─────────▼──────────┐                              │   │
│  │              │   API Gateway      │  ← 표준 REST + JWT 인증      │   │
│  │              │   (Kong / Istio)   │                              │   │
│  │              └─────────┬──────────┘                              │   │
│  └────────────────────────┼─────────────────────────────────────────┘   │
└───────────────────────────┼─────────────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────────────┐
│                      통합 분석·검증 플랫폼 (신규 개발)                        │
│  ┌─────────────────────┐    ┌──────────────────────────────────────┐   │
│  │  KDeepMatrixAI      │◄──►│  Synthetic Data Quality Platform    │   │
│  │  (분석 실행)        │    │  (합성데이터 생성·검증)              │   │
│  │                     │    │                                      │   │
│  │  • RAM/수명/IMQC    │    │  • Statistical/Perturbation/SDV    │   │
│  │    분석 수행        │    │  • 6차원 품질 검증                 │   │
│  │  • 결과 시각화      │    │  • TSTR ML Utility                 │   │
│  │  • 보고서 출력      │    │  • 프라이버시 검증                 │   │
│  └─────────────────────┘    └──────────────────────────────────────┘   │
│           │                              │                              │
│           └──────────────┬───────────────┘                              │
│                          ▼                                              │
│              ┌─────────────────────┐                                    │
│              │   데이터 브로커     │  ← 합성데이터를 분석모델 입력으로    │
│              │   (Data Broker)     │     자동 변환/매핑                 │
│              └─────────────────────┘                                    │
│                          │                                              │
│           ┌──────────────┼──────────────┐                               │
│           ▼              ▼              ▼                               │
│      ┌────────┐   ┌──────────┐   ┌──────────┐                         │
│      │ 실데이터│   │ 합성데이터│   │ 시뮬레이션│  ← 3자 비교 데이터셋   │
│      │ (xlsb)│   │ (csv)    │   │ 데이터    │                         │
│      └────────┘   └──────────┘   └──────────┘                         │
│           │              │              │                               │
│           └──────────────┼──────────────┘                               │
│                          ▼                                              │
│              ┌─────────────────────┐                                    │
│              │   검증 시나리오     │  ← 자동화된 A/B 테스트 파이프라인  │
│              │   엔진              │                                    │
│              │                     │  ① 실데이터로 학습 → 실데이터 테스트 │
│              │   • 학습 파이프라인 │  ② 합성데이터로 학습 → 실데이터 테스트│
│              │   • 평가 파이프라인 │  ③ 시뮬레이션으로 학습 → 실데이터 테스트│
│              │   • 리포트 생성     │     → 3가지 결과를 한 화면 비교     │
│              └─────────────────────┘                                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 신규 개발 필요 모듈

| # | 모듈명 | 기능 | 기술 스택 | 우선순위 |
|---|---|---|---|---|
| 1 | **Model Trainer** | 001/002/007을 ML 모델로 재학습 (004/005는 완료) | scikit-learn, XGBoost, lifelines | P0 |
| 2 | **Model Exporter** | 학습된 모델을 `.pkl`/`.joblib`/`.onnx`로 export | skl2onnx, joblib | P0 |
| 3 | **Model Serving API** | 5개 모델 전체를 FastAPI/BentoML로 통합 Serving | BentoML, FastAPI | P0 |
| 4 | **Data Broker** | 합성데이터 출력 → 분석모델 입력 형식으로 자동 변환 | pandas, pydantic | P1 |
| 5 | **A/B Test Engine** | 3가지 데이터셋으로 학습한 모델의 성능을 자동 비교 | pytest-benchmark, mlflow | P1 |
| 6 | **Scenario Runner** | 검증 시나리오를 YAML로 정의하고 자동 실행 | Prefect, Airflow | P1 |
| 7 | **K8s Manifest** | Docker 이미지 빌드 + K8s 배포 YAML | Docker, Helm | P2 |

---

## 4. 단계별 준비 로드맵 (제안)

### Phase 1: 핵심 모델 Binary화 (2~3주)
- [x] af_ba_req_004(시뮬레이션) 완전 구현 + GBR 학습 → `.joblib` 산출 ✅
- [x] af_ba_req_005(유사부품) KMeans+TF-IDF → `joblib` 저장 + 코사인 유사도 API화 ✅
- [ ] af_ba_req_002(수명예측) scipy 분포적합 → `sklearn` 기반 예측 모델로 재구성
- [ ] af_ba_req_001(RAM) lifelines → 생존분석 ML 모델로 재구성
- [ ] af_ba_req_007(IMQC) 집계 로직 → 회귀/분류 모델로 재구성
- [ ] Model Serving API (BentoML) 프로토타입 개발

### Phase 2: 데이터 연계 및 검증 시나리오 (2주)
- [ ] Data Broker 개발: 합성데이터 CSV → af_ba_req_* 입력 형식 자동 변환
- [ ] "실데이터 vs 합성데이터 vs 시뮬레이션" 3자 비교 파이프라인 구축
- [ ] TSTR 프레임워크를 KDeepMatrixAI 모델에도 적용 (합성데이터로 학습 → 실데이터로 테스트)
- [ ] 검증 시나리오 3종 YAML 정의 + 자동 실행 엔진

### Phase 3: 통합 플랫폼 및 시연 데모 (2주)
- [ ] KDeepMatrixAI + Synthetic Data Quality Platform 단일 포털 통합
- [ ] Streamlit 멀티페이지: "합성데이터 생성 → 품질검증 → 분석모델 실행 → 결과비교" 원스톱 흐름
- [ ] 국방지능형플랫폼 API 스펙 준수 어댑터 개발
- [ ] Docker 이미지화 + Kubernetes 배포 매니페스트

### Phase 4: 고도화 (사업 수주 후)
- [ ] MLOps 파이프라인 (Airflow/Prefect): 데이터 수집 → 학습 → 배포 → 모니터링 → 재학습
- [ ] 고급 합성데이터 (CTGAN, Diffusion) 도입
- [ ] 차등 프라이버시 (Differential Privacy) 메트릭 추가
- [ ] 실시간 모델 모니터링 (Drift Detection)

---

## 5. 시연 시나리오 제안 (데모 구성)

### 시나리오 A: "합성데이터로 학습한 RAM 모델의 신뢰성 검증"
```
[Step 1] 실데이터(가용도분석자료.xlsb) 업로드
    ↓
[Step 2] 합성데이터 생성 (perturbation 모드, 327,699행 동일 규모)
    ↓
[Step 3] 품질 검증 실행 → PASS 판정 확인
    ↓
[Step 4] 실데이터로 RAM 분석 실행 → MTBF=330.66, Availability=0.9428
    ↓
[Step 5] 합성데이터로 동일 RAM 분석 실행 → 지표 비교
    ↓
[Step 6] 두 결과를 나란히 시각화 + 차이율 리포트 출력
```

### 시나리오 B: "시뮬레이션 역추정 모델의 Serving 데모"
```
[Step 1] 시험소별 작업량 예측 모델(af_ba_req_004)을 BentoML로 Serving
    ↓
[Step 2] 입력: 난이도, 기술등급, 효율%, 정비주기, 소모인시
    ↓
[Step 3] 예측: 주간 작업량 + Bootstrap 90% CI + Conformal Prediction PI
    ↓
[Step 4] 역추정: 목표 작업량 달성을 위한 최적 입력 변수 자동 산출
    ↓
[Step 5] 결과를 Plotly 대시보드로 실시간 시각화
```

### 시나리오 C: "3자 데이터 A/B 테스트"
```
[Step 1] 동일 모델(예: XGBRegressor)을 3가지 데이터셋으로 각각 학습
    ├─ Model-Real: 실데이터 학습
    ├─ Model-Syn: 합성데이터 학습
    └─ Model-Sim: 시뮬레이션 데이터 학습
    ↓
[Step 2] 동일 테스트셋(실데이터)로 3 모델 Inference
    ↓
[Step 3] 성능 비교 (RMSE, MAE, R²) + 통계적 유의성 검정
    ↓
[Step 4] "합성데이터가 실데이터의 95% 성능을 달성함" 등의 정량적 근거 도출
```

---

## 6. 결론 및 제언

### 6.1 현재 위치
- **합성데이터 플랫폼**: 생성·검증은 세계적으로도 경쟁력 있는 수준
- **분석 통합 플랫폼**: **5개 모델 전체 완전 구현** (001/002/004/005/007)
  - 001/002/007: 코드 기반 분석 + 시각화 + Streamlit UI
  - 004/005: ML 모델 Binary + 실시간 Serving API + Streamlit UI
- **연결고리**: 004 모델에서 **"합성데이터 → AI 모델 학습 → Serving"** 파이프라인이 실증됨
  - 3자 비교(실데이터 vs 합성데이터 vs 시뮬레이션)는 Streamlit 멀티페이지 통합 필요

### 6.2 최우선 과제 (즉시 실행 필요)
1. **001/002/007 Binary화**: 3개 코드 기반 모델을 ML 모델로 재구현하여 `.joblib` 산출
2. **통합 Model Serving Layer**: BentoML 기반으로 5개 모델 전체를 실시간 Inference 데모 확보
3. **3자 비교 시나리오 파이프라인**: "실데이터 vs 합성데이터 vs 시뮬레이션"을 한 화면에서 보여주는 데모는 **제안 발표의 핵심 임팩트**

### 6.3 사업 제안 전략
- **차별화 포인트**: "단순 합성데이터 생성"이 아닌 **"합성데이터 기반 국방 AI 모델 학습·검증·배포 전 주기 플랫폼"**
- **실증 근거**: TSTR + 3자 A/B 테스트로 "합성데이터가 실전 투입 가능하다"는 정량적 근거 확보
- **확장성**: Adapter 패턴으로 향후 새로운 국방 분석모델을 무한히 추가 가능함을 강조

---

## 7. 첨부: 참고 리포지토리

| 플랫폼 | URL | 역할 |
|---|---|---|
| 합성데이터 생성·검증 | https://github.com/kbitone-bot/synthetic-data-quality-platform | 데이터 수급 및 품질 보증 |
| 빅데이터 분석·가시화 | https://github.com/kbitone-bot/KDeepMatrixAI | 분석모델 통합 실행 및 시각화 |

---

> **다음 회의까지 준비할 것**:  
> ① 001/002/007 3개 모델 Binary화 + 통합 Serving API 데모  
> ② "실데이터 vs 합성데이터 vs 시뮬레이션" 3자 비교 Streamlit 화면  
> ③ 통합 아키텍처 다이어그램 (PPT용) 1장
